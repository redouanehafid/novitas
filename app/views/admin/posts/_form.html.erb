<%= simple_form_for [:admin, @post] do |f| %>
<%= f.input :online %>
<%= f.input :title %>
<%= f.input :content %>
<%= f.button :submit, @post.new_record? ? "Ajouter" : "Modifier", class: "btn btn-primary" %>
<% end %>